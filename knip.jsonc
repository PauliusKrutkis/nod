{
  "$schema": "https://unpkg.com/knip@6/schema-jsonc.json",
  // Suppress exports tagged @lintignore in source (see knip docs).
  "tags": ["-lintignore"],
  // Rust/Tauri build output — not part of the JS/TS project graph. The Nexos
  // probe (docs/AI.md) and the ledger blame probe (docs/LEDGER.md §12) are
  // standalone dev scripts run directly with node, never imported.
  "ignoreFiles": [
    "**/src-tauri/target/**",
    "scripts/probe-nexos.mjs",
    "scripts/probe-ledger-blame.mjs"
  ],
  // Both are CLI-only: nothing imports them, they are invoked as commands
  // (wrangler for the Pages secrets and bindings in docs/LAUNCH.md).
  "ignoreDependencies": ["@biomejs/biome", "wrangler"],
  // Cloudflare Pages Functions: each file is a route handler invoked by the
  // platform, not imported from anywhere in the JS graph — the Astro plugin
  // only knows about src/pages entries.
  "workspaces": {
    // Capture scenes run through their own playwright config, which the
    // playwright plugin does not auto-detect (it only knows
    // playwright.config.ts).
    "apps/desktop": {
      "entry": ["playwright.capture.config.ts", "e2e/capture/*.capture.ts"]
    },
    "apps/gallery": {
      "entry": ["e2e/*.shots.ts"]
    },
    "apps/web": {
      // scripts/: operator-run one-offs (docs/LAUNCH.md), not imported.
      "entry": ["functions/**/*.ts!", "scripts/*.mjs"]
    }
  }
}
