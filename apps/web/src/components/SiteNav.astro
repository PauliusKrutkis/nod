---
interface Props {
  cta: { href: string; label: string };
}

const { cta } = Astro.props;
---

<header class="nav">
  <div class="wide nav__inner">
    <a class="brand" href="/">
      <span class="brand__mark">nod</span>
      <span class="brand__dim">— pr review</span>
    </a>
    <a class="nav__link" href={cta.href}>{cta.label}</a>
  </div>
</header>
